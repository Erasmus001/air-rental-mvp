import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ResetEmailScreen = () => {
	return (
		<View>
			<Text>ResetEmailScren</Text>
		</View>
	);
};

export default ResetEmailScreen;

const styles = StyleSheet.create({});
