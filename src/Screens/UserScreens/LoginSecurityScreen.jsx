import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const LoginSecurityScreen = () => {
	return (
		<View>
			<Text>LoginSecurityScreen</Text>
		</View>
	);
};

export default LoginSecurityScreen;

const styles = StyleSheet.create({});
